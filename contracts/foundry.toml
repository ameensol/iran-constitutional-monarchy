[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.20"
optimizer = true
optimizer_runs = 200
evm_version = "paris"
ffi = true

# Use `FOUNDRY_PROFILE=deploy forge build` for production-optimized builds
[profile.deploy]
via_ir = true
